#include <bits/stdc++.h>
using namespace std;
int main()
{
    int a, b;
    vector<int> v;
    cin >> a >> b;
    for (int i = 1; i <=a && i<=b; i++)
    {
        if ((a % i == 0) && (b % i == 0))
                
        

    }
    sort(v.begin(), v.end());
    cout << v[0];

    return 0;
}